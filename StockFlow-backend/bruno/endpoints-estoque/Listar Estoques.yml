info:
  name: Listar Estoques
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/estoques
  auth:
    type: bearer
    token:

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
