info:
  name: Cadastrar Estoque
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:8080/api/estoques
  body:
    type: json
    data: |-
      {
        "nome": "Estoque Central",
        "empresaId": 1
      }
  auth:
    type: bearer
    token:

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
