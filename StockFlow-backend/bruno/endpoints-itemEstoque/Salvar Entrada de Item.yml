info:
  name: Salvar Entrada de Item
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/itens-estoque/1/entrada
  body:
    type: json
    data: |-
      {
       "quantidade": 3 
      }
  auth:
    type: bearer
    token:

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
