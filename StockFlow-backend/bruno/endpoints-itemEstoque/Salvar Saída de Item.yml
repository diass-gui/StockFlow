info:
  name: Salvar Saída de Item
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/api/itens-estoque/1/saida
  body:
    type: json
    data: |-
      {
        "quantidade": 4
      }
  auth:
    type: bearer
    token:

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
