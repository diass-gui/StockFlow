info:
  name: Cadastrar Item de Estoque
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/itens-estoque
  body:
    type: json
    data: |-
      {
        "estoqueId": 1,
        "produtoId": 3,
        "quantidade": 5
      }
  auth:
    type: bearer
    token:

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
