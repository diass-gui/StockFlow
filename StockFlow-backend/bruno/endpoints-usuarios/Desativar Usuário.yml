info:
  name: Desativar Usuário
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:8080/api/usuarios/desativar
  auth:
    type: bearer
    token:

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
