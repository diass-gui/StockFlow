info:
  name: Buscar Usuario pelo ID
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/usuarios/1
  auth:
    type: bearer
    token:

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
