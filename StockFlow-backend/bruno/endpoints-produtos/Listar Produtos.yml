info:
  name: Listar Produtos
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/produtos
  auth:
    type: bearer
    token:

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
