info:
  name: Buscar Produto pelo ID
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8080/api/produtos/2
  auth:
    type: bearer
    token:

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
