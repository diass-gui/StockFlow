info:
  name: Remover Produto pelo ID
  type: http
  seq: 8

http:
  method: DELETE
  url: http://localhost:8080/api/produtos/remover/dois
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
